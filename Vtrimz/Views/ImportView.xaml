<UserControl x:Class="Vtrimz.Views.ImportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BgDarkBrush}">
    <Grid>
        <Border Background="{StaticResource BgPanelBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="12"
                Padding="48"
                MaxWidth="520"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <StackPanel>
                <TextBlock Text="Import Media"
                           FontSize="28"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource TextPrimaryBrush}"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="Choose a video to open the editor"
                           FontSize="14"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           HorizontalAlignment="Center"
                           Margin="0,8,0,32"
                           TextAlignment="Center"/>

                <Button x:Name="SelectVideoButton"
                        Content="Choose Video"
                        Style="{StaticResource EditorButton}"
                        FontSize="15"
                        Padding="24,12"
                        HorizontalAlignment="Center"
                        Click="SelectVideoButton_Click"/>

                <TextBlock x:Name="SelectedFileText"
                           Text="No file selected"
                           FontSize="12"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           HorizontalAlignment="Center"
                           Margin="0,20,0,0"
                           TextWrapping="Wrap"
                           TextAlignment="Center"
                           MaxWidth="400"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
